@model IEnumerable<ProjetoGuru.Models.UsuarioCategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UsuarioCategoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UsuarioCategoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UsuarioCategoriaID })
        </td>
    </tr>
}

</table>
